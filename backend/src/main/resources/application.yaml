spring:
  application:
    name: fullstack-app


  datasource:
    url: jdbc:postgresql://192.168.1.200:5455/tododb?socketTimeout=30
    username: admin
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true